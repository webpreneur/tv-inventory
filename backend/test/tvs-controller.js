const expect = require('chai').expect;
const sinon = require('sinon');

const { TV } = require('../models');
const {
    getTVs,
    createTV,
    getTV,
    updateTV,
    deleteTV
} = require('../controllers/tvs');

describe('TVs Controller - getTV', function() {
    it('should send a response with an existing TV', function(done) {
        
    });
});